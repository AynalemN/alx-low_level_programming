#include "main.h"
/**
 * mul - is a function that multiplies two integers.
 * @a: the first number
 * @b: the second number
 * Return: the multiplication result 
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
