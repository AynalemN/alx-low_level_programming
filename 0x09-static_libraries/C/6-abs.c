#include "main.h"
/**
 * _abs - prints the absolute value of an integer.
 *
 * @j: arguments to be checked.
 *
 * Return: absolute value.
 */
int _abs(int j)
{
	if (j < 0)
	{
		j = -1 * j;
	}
	return (j);
}
