#include "main.h"
/**
 * add -adds two numbers.
 * @a: the first integer number.
 * @b: the second integer numbers
 * Return: the sum of the two numbers
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
